/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MenuPipe } from './menu.pipe';

describe('MenuPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuPipe();
    expect(pipe).toBeTruthy();
  });
});
